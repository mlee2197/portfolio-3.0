import React from "react";
import { Section } from "../components";

interface CatProps {
  
};

const Cat: React.FC<CatProps> = () => {
  return (
    <Section>
      Cat
    </Section>
  );
};

export { Cat };